# TikTok cookie checker

#### flags:
> * -t: threads count
> * -c: cookie folder
> * -r: result folder

#### example:

> py main.py -t 2 -c cookie -r result

#### default args:
> * threads:1
> * cookie folder:cookie
> * result folder:result
